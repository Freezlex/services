apiVersion: v1
kind: Service
metadata:
  name: factorio
  namespace: games
spec:
  type: ClusterIP
  selector:
    app: factorio
  ports:
    - name: factorio
      protocol: UDP
      port: 34197
      targetPort: 34197
