apiVersion: v1
kind: Service
metadata:
  name: traefik-web
  namespace: reverse-proxy
spec:
  type: LoadBalancer
  ports:
    - name: web
      targetPort: web
      port: 80
  selector:
    app: traefik
---
apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  namespace: reverse-proxy
  labels:
    app: traefik-dashboard
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: traefik
