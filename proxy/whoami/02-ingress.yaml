apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
  namespace: proxy
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`whoami.polarys.im`)
      services:
        - kind: Service
          name: whoami
          port: 80
