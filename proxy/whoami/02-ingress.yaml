apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
  namespace: proxy
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`whoami.julien-ivs.com`)
      services:
        - kind: Service
          name: whoami
          port: 80
