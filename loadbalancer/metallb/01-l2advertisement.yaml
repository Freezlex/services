apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: homelab-l2
  namespace: metallb
spec:
  ipAddressPools:
    - homelab-pool
  # If you have multiple NICs on your k3s node and want to restrict which
  # interface answers ARP, uncomment and fill in:
  # interfaces:
  #   - eth0
